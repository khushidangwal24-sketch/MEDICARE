services:
  mongo:
    image: mongo:7
    container_name: healthhub-mongo
    ports:
      - "27017:27017"
    volumes:
      - healthhub_mongo_data:/data/db
    restart: unless-stopped

volumes:
  healthhub_mongo_data:
